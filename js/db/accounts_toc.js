define(function(require) {

/**
 * Provides a list of all accounts known to the system.  These accounts need not
 * be loaded/active in memory.  (This differs from v1 where an account had to
 * be loaded to be reported, limiting our capability to lazy-load.)
 */
function AccountsTOC() {

}
AccountsTOC.prototype = {

};

return AccountsTOC;
});

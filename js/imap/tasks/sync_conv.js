define(function(require) {

/**
 * Given a conversation-id (X-GM-THRID), synchronize its state by going in the
 * all mail folder, issuing a SEARCH on the conversation, doing message delta
 * 
 */

});

define(function() {
'use strict';

/**
 * Clean up after what we did in "conv_client_decorator.js".
 */
return function cleanupConversation(/*mailConversation*/) {
  // We assume a super boring app for testing that does not give a fig about
  // interesting things like tidbits.
};
});

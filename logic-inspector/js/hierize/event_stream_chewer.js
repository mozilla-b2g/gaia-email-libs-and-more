
/**
 * Stateful processor of events, fed to us in batches via calls to `chewEvents`.
 */
class EventStreamChewer {
  constructor(config) {

  }

  chewEvents(rawEvents) {
    for (let rawEvent of rawEvents) {
    }
  }
}

import React from 'react';

// NB: The contents of this file should likely be exploded into a file-per-class
// directory at some point.  Running with a single file right now based on
// :mcav's original work with the test views to see how it works out.  It's
// a given that at some point I (:asuth) will complicate this so much that we
// have to split out, but I'm interested to see when that is.

/**
 * The master timeline widget
 */
export class TimelineView extends React.Component {
  constructor(props) {
    super(props);
  }
}

class HierarchyBox extends React.Component {
  constructor(props) {
    super(props);
  }
}

/**
 * Multi-mode display container for runs of events that we have coalesced into
 * a group for display purposes.  For example, a run of individually logged
 * IMAP protocol lines
 */
class CoalescedEventRun extends React.Component {
  constructor(props) {
    super(props);
  }
}

/**
 *
 */
class Lifeline extends React.component {
  constructor(props) {

  }
}
